# Description: Exemple of .env file for the front-end

# The url to the backend api where the front-end will send requests (Vite requires the VITE_ prefix)
# Default (production with reverse-proxy): "http://localhost/api/v1"
# Default (dev backend direct): "http://localhost:4200/api/v1"
# VITE_API_URL='http://dev.junqo.fr:4200/api/v1'

# Optional: WebSocket URL (also requires VITE_ prefix)
# VITE_WS_URL='http://dev.junqo.fr:4200'
