export enum WorkContext {
  // Work is performed entirely at the company's physical location
  ON_SITE = 'ON_SITE',

  // Work is split between remote and on-site locations
  HYBRID = 'HYBRID',

  // Work is performed entirely from a remote location
  REMOTE = 'TELEWORKING',
}
